# dataset
root: datasets
split_rate: 0.7
num_classes: 10

# model
# ckpt: weights\last_ckpt_default.pth
save_name: default
save_suffix: pth

# train
gpus: []
epochs: 50
batch_size: 16
lr: 0.001

# run command: python demo.py --config config_example.py

